# Security and sensitive-data reporting

Do not open a public issue containing production Stage data, raw Prim paths,
resolver configuration, reverse token maps, credentials, or private research
inputs. Report a suspected publication-boundary failure privately to the
repository owner.

The public artifact accepts only pair-tokenized projections and fails closed on
schema, input hash, manifest, expected-result, and privacy mismatches.
